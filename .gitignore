node_modules/
.DS_Store

# tokens never land in the tree
.env
.env.*

# build output — emitted at publish time (prepack), never committed. Dev runs
# the .ts sources directly via Node type-stripping; only the published tarball
# carries .js, because Node refuses to strip types under node_modules.
dist/
